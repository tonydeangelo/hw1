Week 1 homework took me 6 hours to complete.

I have complete the following required fields:

* Add a container view and modify properties: backgroundColor, cornerRadius, font, textcolor, 
* Add buttons with different images for default and highlighted
* Add UIImageViews and chose the correct mode
* Add a UITextField with a background image
* Add UILabels with attributed text so that words can be bolded
